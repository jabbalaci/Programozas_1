#include "prog1.h"
#include <stdio.h>
#include <string.h>

int main()
{
    // string nev = get_string("Neved: ");

    // printf("Hello %s!\n", nev);

    int n = 3;
    printf("%d kétszerese: %d\n", n, duplaz(n));

    return 0;
}
